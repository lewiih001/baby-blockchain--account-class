class Account  {
    constructor(Id = "", data = []) {
        this.timestamp = timestamp;
        this.data = data;
        this. balance = balance;
        this. keypair = new keypair;
        this.update  = new balance;
        this.hash = this.getHash();
        this.prevHash = ""; 
        this.nonce = 0;
    }

}
const balance = () =>{
    console.log(`Initial balance ${add } to get current balance else subtract to get current balance`);
}

const Account = (implement) =>{
        let keypair = KeySignew();
        let public_key = hexencode
        keypair.get_public_key
}
const  keypair = (add) =>{
keysig.selfwallet.push
}
const update = (balance) =>{
self.balance
}
const payment = (create) =>{
    
}

const print = (balance) =>{
    console.log("Balance: {}", self.balance);
}
const sign = (data) =>{
    console.log(Id)
}